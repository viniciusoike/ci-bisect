name: caller-minimal

on:
  workflow_dispatch:
  pull_request:

jobs:
  call:
    uses: viniciusoike/ci-bisect/.github/workflows/pkgdown.yml@main
    permissions:
      contents: read
      pages: write
      id-token: write
